name: CI

on:
  push:
    branches: [ master ]

jobs:
  quick-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6
      - name: make
        run: make
      - name: make check
        run: make check
